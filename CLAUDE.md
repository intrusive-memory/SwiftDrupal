# Claude Code Instructions

See [AGENTS.md](AGENTS.md) for project context, layout, conventions, and
build/install instructions, and [docs/cli-contract.md](docs/cli-contract.md)
for the CLI's agent-facing contract.

Work on the `development` branch (or feature branches off it); `main`
changes only through PRs from `development`.
