// Entry point for the `drupal` binary. All command logic lives in the
// `SwiftDrupal` library target; this file only hands control to the root
// `Drupal` command.

import SwiftDrupal

@main
extension Drupal {}
